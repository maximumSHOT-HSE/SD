#include "executors/GrepExecutor.h"

#include <cxxopts.hpp>

Status GrepExecutor::execute(const Command &command, StringChannel &inputChannel, StringChannel &outputChannel) const {

    

    return Status();
}
